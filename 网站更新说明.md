# 杨茜律师个人网站更新说明

网站主体内容位于 `index.html`，视觉样式位于 `app/globals.css`。

## 更新案例

打开 `index.html`，找到 `id="cases"` 的区域。复制其中一个 `<article> ... </article>` 区块并修改：

- `<span>`：案例所属业务类型
- `<h3>`：案例标题
- `<p>`：案例简介（建议保持匿名，避免披露客户信息）

## 更新研究内容

在同一文件找到 `id="insights"` 的区域，新增或修改一个 `<article>` 区块：

- `<span>`：可填写日期，或“专题研究 / 实务观察”
- `<h3>`：文章标题
- `<p>`：一到两句话摘要

## 更新联系方式

搜索电话号码或邮箱即可统一替换。头像文件位于 `public/yang-qian.png`。

## 发布前检查

确认客户信息已经脱敏，研究文章标题和摘要无误，并在手机上检查电话与邮箱链接是否可以直接打开。
